# Core dependencies
torch>=2.0.0
numpy>=1.20.0

# Optional: BPE tokenizer (requires Rust compiler to build from source)
# If installation fails, use CharTokenizer instead (no external deps)
# tiktoken>=0.5.0
